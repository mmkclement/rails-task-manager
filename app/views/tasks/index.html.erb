<h1>My Task </h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
    <%= task.title %> <%= task.details %> <%= task.completed %>
    <%= link_to "details", task_path(task)%>
    </li>

  <%end%>
</ul>

 <%= link_to "new task", new_task_path%>
